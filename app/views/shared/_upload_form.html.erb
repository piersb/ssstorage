
<%= form_for(@avfile) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.input :title, placeholder: "Title" %>
  </div>
  <%= f.submit "Upload", class: "btn btn-primary" %>
<% end %>